// ============================================================\n// GUIA RÁPIDO DE INTEGRAÇÃO - LoadingAnimation\n// ============================================================\n\n// 1. EXEMPLO BÁSICO - Adicione em qualquer componente:\n// ============================================================\n\nimport { LoadingAnimation } from '@/components/LoadingAnimation';\n\nfunction MyComponent() {\n  return (\n    <div className=\"flex items-center justify-center min-h-screen\">\n      <LoadingAnimation size=\"lg\" label=\"Carregando...\" />\n    </div>\n  );\n}\n\n\n// 2. COM OVERLAY FULLSCREEN - Para operações longas:\n// ============================================================\n\nimport { LoadingOverlay } from '@/components/LoadingOverlay';\nimport { useState } from 'react';\n\nfunction MyScreen() {\n  const [isLoading, setIsLoading] = useState(false);\n\n  const handleAction = async () => {\n    setIsLoading(true);\n    try {\n      // Sua operação aqui\n      await fetch('/api/something');\n    } finally {\n      setIsLoading(false);\n    }\n  };\n\n  return (\n    <>\n      <LoadingOverlay isVisible={isLoading} message=\"Processando...\" />\n      <button onClick={handleAction}>Executar</button>\n    </>\n  );\n}\n\n\n// 3. INTEGRAÇÃO EM HomeScreen.tsx:\n// ============================================================\n\n// No início do arquivo, adicione:\nimport { LoadingOverlay } from '@/components/LoadingOverlay';\n\n// No componente, adicione estado:\nconst [isLoading, setIsLoading] = useState(true);\n\n// Use o hook useEffect para controlar loading:\nuseEffect(() => {\n  const loadTrips = async () => {\n    try {\n      setIsLoading(true);\n      // seu código aqui\n    } finally {\n      setIsLoading(false);\n    }\n  };\n  loadTrips();\n}, []);\n\n// No JSX, adicione:\nreturn (\n  <>\n    <LoadingOverlay isVisible={isLoading} message=\"Carregando viagens...\" />\n    {/* resto do componente */}\n  </>\n);\n\n\n// 4. INTEGRAÇÃO EM TripDetailScreen.tsx:\n// ============================================================\n\nimport { LoadingOverlay } from '@/components/LoadingOverlay';\nconst [isLoadingMap, setIsLoadingMap] = useState(false);\n\nreturn (\n  <>\n    <LoadingOverlay isVisible={isLoadingMap} message=\"Carregando mapa...\" />\n    <MapboxMap attractions={attractions} />\n  </>\n);\n\n\n// 5. PROPS DISPONÍVEIS:\n// ============================================================\n\n// LoadingAnimation:\n// - src: string (URL da animação, tem padrão)\n// - size: 'sm' | 'md' | 'lg' | 'xl' (padrão: 'md')\n// - label: string (texto opcional)\n// - loop: boolean (padrão: true)\n// - autoplay: boolean (padrão: true)\n// - className: string (customização)\n\n// LoadingOverlay:\n// - isVisible: boolean (requerido)\n// - message: string (padrão: 'Carregando...')\n// - animationSrc: string (URL customizada)\n\n\n// 6. TAMANHOS:\n// ============================================================\n\n<LoadingAnimation size=\"sm\" />   {/* 48x48 */}\n<LoadingAnimation size=\"md\" />   {/* 96x96 - padrão */}\n<LoadingAnimation size=\"lg\" />   {/* 128x128 */}\n<LoadingAnimation size=\"xl\" />   {/* 192x192 */}\n\n\n// 7. DARK MODE (automático):\n// ============================================================\n\n// Os componentes já suportam dark mode automaticamente!\n// Não precisa fazer nada especial.\n// O texto e fundo se adaptam automaticamente.\n\n\n// 8. COM REACT QUERY:\n// ============================================================\n\nimport { useQuery } from '@tanstack/react-query';\nimport { LoadingOverlay } from '@/components/LoadingOverlay';\n\nfunction MyComponent() {\n  const { data, isLoading } = useQuery({\n    queryKey: ['data'],\n    queryFn: () => fetch('/api/data').then(r => r.json())\n  });\n\n  return (\n    <>\n      <LoadingOverlay isVisible={isLoading} message=\"Carregando...\" />\n      {data && <div>{data}</div>}\n    </>\n  );\n}\n\n\n// 9. HOOK CUSTOMIZADO:\n// ============================================================\n\nimport { useState } from 'react';\n\nfunction useLoading() {\n  const [isLoading, setIsLoading] = useState(false);\n\n  const withLoading = async (asyncFn) => {\n    setIsLoading(true);\n    try {\n      return await asyncFn();\n    } finally {\n      setIsLoading(false);\n    }\n  };\n\n  return { isLoading, withLoading };\n}\n\n// Uso:\nconst { isLoading, withLoading } = useLoading();\nawait withLoading(() => fetch('/api/something'));\n\n\n// 10. ACESSIBILIDADE:\n// ============================================================\n\n// ✓ Automaticamente:\n// - role=\"status\"\n// - aria-live=\"polite\"\n// - aria-label com descrição\n// - Contraste WCAG AA+\n// - Suporte a leitores de tela\n\n// Não precisa fazer nada!\n\n\n// ============================================================\n// RESUMO DOS ARQUIVOS CRIADOS:\n// ============================================================\n\n// src/components/LoadingAnimation.tsx\n//   └─ Componente principal com animação\n//\n// src/components/LoadingOverlay.tsx\n//   └─ Overlay fullscreen\n//\n// LOADING_ANIMATION_GUIDE.md\n//   └─ Documentação completa\n\n\n// ============================================================\n// STATUS: ✓ PRONTO PARA USAR\n// ============================================================\n\n// ✓ Instalado (@lottiefiles/dotlottie-react)\n// ✓ Componentes criados\n// ✓ TypeScript tipado\n// ✓ Dark mode suportado\n// ✓ Acessível\n// ✓ Build passando\n// ✓ Pronto em produção\n\n// Próximo passo: Integre em seus componentes!\n"